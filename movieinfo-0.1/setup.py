from distutils.core import setup
setup(
        name = 'movieinfo',
        packages = ['movieinfo'],
        version = '0.1',
        description = 'looks up all movies/ TV shows in a folder and gets the  genre, runtime, plot and IMDB and RottenTomatoes ratings of the movie and stores it as a csv file.',
        author = 'Anubhab Sen',
        author_email = 'anubhabsen@gmail.com',
        url = 'https://github.com/anubhabsen/movieinfo',
        download_url = 'https://github.com/anubhabsen/movie-info/archive/0.1.tar.gz', # I'll explain this in a second
        keywords = ['movie', 'data', 'info', 'information', 'scrap'],
        classifiers = [],
        )
